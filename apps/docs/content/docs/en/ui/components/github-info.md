---
title: GitHub info
description: Link a repository and display its current star count.
---

# GitHub info

```mdx
<GithubInfo owner="tarkaworks" repo="foldocs" />
```

The card uses the official GitHub mark, opens the repository in a new tab, and
loads a lightweight star badge lazily. Use `repository` instead of `repo` when
that reads better in generated content.
