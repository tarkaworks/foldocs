---
title: Quick Start
description: Create a Foldocs application and start authoring documentation.
icon: rocket
order: 1
---

# Quick Start

<Badge>Foldkit native</Badge>

Foldocs turns Markdown and deterministic MDX into a complete Foldkit documentation
application with typed content, navigation, full-text search, and AI-readable output.

Every document is also available as processed Markdown by appending `.md` to its
URL, with copy and view actions generated automatically.

<Callout title="One command, one opinionated stack">
  Run `pnpm create foldocs my-docs`. There is no framework picker: Foldkit is
  the application framework and Effect powers the runtime.
</Callout>

## Automatic installation

Create a complete Foldocs application with Foldkit, Effect, local search, static
output, Markdown routes, and LLM files already configured:

```npm
npm create foldocs@latest my-docs
cd my-docs
npm run dev
```

## What you get

<Cards>
  <Card title="Authoring" href="/en/docs/page-conventions">
    Markdown, MDX components, frontmatter, tables, task lists, and highlighted
    code.
  </Card>
  <Card title="Search" href="/en/docs/search">
    Browser-local Orama search by default with a provider-neutral Effect
    contract.
  </Card>
</Cards>

## Typed all the way down

```ts
import { createDocsProgram } from 'foldocs'
import {
  basePath,
  i18n,
  manifest,
  navigations,
  searchIndexUrls,
  siteConfig,
} from 'virtual:foldocs'

const program = createDocsProgram({
  basePath,
  i18n,
  manifest,
  navigations,
  searchIndexUrls,
  site: siteConfig,
})
```

The virtual manifest eagerly contains serializable metadata and lazy-loads each
compiled document only when its route is visited.

## Static from the first response

Production builds prerender every landing and documentation route. Direct
navigation receives the real page rather than a temporary loading document.
