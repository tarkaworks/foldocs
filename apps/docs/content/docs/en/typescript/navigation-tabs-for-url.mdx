---
title: 'navigationTabsForUrl'
description: 'Returns layout tabs when the URL is inside a root folder.'
order: 46
tags:
  - TypeScript
  - 'Variable'
---

# navigationTabsForUrl

Returns layout tabs when the URL is inside a root folder.

**Variable** · `navigation`

```ts twoslash
/** Returns layout tabs when the URL is inside a root folder. */
export declare const navigationTabsForUrl: (
  nodes: ReadonlyArray<NavigationNode>,
  currentUrl: string,
) => ReadonlyArray<NavigationTab>
```
