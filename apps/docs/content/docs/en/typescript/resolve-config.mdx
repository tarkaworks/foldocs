---
title: 'resolveConfig'
description: 'Validates a `FoldocsConfig` and fills in every default, producing the config the build consumes.'
order: 54
tags:
  - TypeScript
  - 'Variable'
---

# resolveConfig

Validates a `FoldocsConfig` and fills in every default, producing the config the build consumes.

**Variable** · `config`

```ts twoslash
/** Validates a `FoldocsConfig` and fills in every default, producing the config the build consumes. */
export declare const resolveConfig: (
  config: FoldocsConfig,
) => ResolvedFoldocsConfig
```
