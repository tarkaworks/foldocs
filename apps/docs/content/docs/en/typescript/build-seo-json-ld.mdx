---
title: 'buildSeoJsonLd'
description: 'Builds the Schema.org graph embedded into every prerendered Foldocs route.'
order: 5
tags:
  - TypeScript
  - 'Variable'
---

# buildSeoJsonLd

Builds the Schema.org graph embedded into every prerendered Foldocs route.

**Variable** · `seo`

```ts twoslash
/** Builds the Schema.org graph embedded into every prerendered Foldocs route. */
export declare const buildSeoJsonLd: (
  input: SeoJsonLdInput,
) => Readonly<Record<string, unknown>> | undefined
```
