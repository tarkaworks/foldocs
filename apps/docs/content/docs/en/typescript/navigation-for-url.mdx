---
title: 'navigationForUrl'
description: 'Returns the sidebar tree visible for a URL, respecting configured roots.'
order: 38
tags:
  - TypeScript
  - 'Variable'
---

# navigationForUrl

Returns the sidebar tree visible for a URL, respecting configured roots.

**Variable** · `navigation`

```ts twoslash
/** Returns the sidebar tree visible for a URL, respecting configured roots. */
export declare const navigationForUrl: (
  nodes: ReadonlyArray<NavigationNode>,
  currentUrl: string,
) => ReadonlyArray<NavigationNode>
```
