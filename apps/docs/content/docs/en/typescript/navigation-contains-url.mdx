---
title: 'navigationContainsUrl'
description: 'Returns whether a page or folder contains the canonical URL.'
order: 34
tags:
  - TypeScript
  - 'Variable'
---

# navigationContainsUrl

Returns whether a page or folder contains the canonical URL.

**Variable** · `navigation`

```ts twoslash
/** Returns whether a page or folder contains the canonical URL. */
export declare const navigationContainsUrl: (
  node: NavigationNode,
  currentUrl: string,
) => boolean
```
