---
title: 'serializeJsonLd'
description: 'Serializes JSON-LD without allowing metadata to terminate the script node.'
order: 70
tags:
  - TypeScript
  - 'Variable'
---

# serializeJsonLd

Serializes JSON-LD without allowing metadata to terminate the script node.

**Variable** · `seo`

```ts twoslash
/** Serializes JSON-LD without allowing metadata to terminate the script node. */
export declare const serializeJsonLd: (
  value: Readonly<Record<string, unknown>>,
) => string
```
