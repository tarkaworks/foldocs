---
title: 'Get a document'
description: 'Returns detailed metadata for a specific document.'
label: 'GET /documents/{documentId}'
order: 4
tags:
  - 'Documents'
---

# Get a document

`GET /documents/{documentId}`

Returns detailed metadata for a specific document.

## Parameters

| Name         | In   | Required | Type           | Description                |
| ------------ | ---- | -------- | -------------- | -------------------------- |
| `documentId` | path | Yes      | `string<uuid>` | Unique document identifier |

<ApiPlayground
  id="getdocument"
  method="GET"
  url="https://api.foldocs.dev/documents/{documentId}"
  body=""
/>

## Request examples

```bash tab="cURL"
curl --request GET --url "https://api.foldocs.dev/documents/{documentId}"
```

```ts tab="TypeScript"
const response = await fetch('https://api.foldocs.dev/documents/{documentId}', {
  method: 'GET',
})

const data = await response.json()
```

```python tab="Python"
import requests

response = requests.request("GET", "https://api.foldocs.dev/documents/{documentId}")
data = response.json()
```

```go tab="Go"
request, err := http.NewRequest("GET", "https://api.foldocs.dev/documents/{documentId}", nil)
if err != nil { panic(err) }
response, err := http.DefaultClient.Do(request)
if err != nil { panic(err) }
defer response.Body.Close()
```

## Responses

### 200 Document details

`application/json`

| Field         | Type                | Required | Description                      |
| ------------- | ------------------- | -------- | -------------------------------- |
| `id`          | `string<uuid>`      | Yes      | Unique document identifier       |
| `slug`        | `string`            | Yes      | URL-friendly document identifier |
| `title`       | `string`            | Yes      | Document title                   |
| `description` | `string`            | No       | Optional document description    |
| `content`     | `string`            | Yes      | Markdown or MDX content          |
| `locale`      | `string`            | No       | Content locale                   |
| `tags`        | `array`             | No       | Content tags for organization    |
| `status`      | `string`            | No       | Document status                  |
| `projectId`   | `string<uuid>`      | Yes      | Parent project identifier        |
| `publishedAt` | `string<date-time>` | No       | Publication timestamp            |
| `createdAt`   | `string<date-time>` | Yes      | Creation timestamp               |
| `updatedAt`   | `string<date-time>` | Yes      | Last update timestamp            |

```json
{
  "id": "doc_a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "slug": "getting-started",
  "title": "Getting Started",
  "description": "Learn how to set up and use Foldocs.",
  "content": "# Getting Started\n\nWelcome to Foldocs...",
  "locale": "en",
  "tags": ["guide", "beginner"],
  "status": "published",
  "projectId": "string",
  "publishedAt": "string",
  "createdAt": "string",
  "updatedAt": "string"
}
```

### 404 Resource not found

`application/json`

| Field     | Type     | Required | Description                        |
| --------- | -------- | -------- | ---------------------------------- |
| `code`    | `string` | Yes      | Machine-readable error code        |
| `message` | `string` | Yes      | Human-readable error message       |
| `details` | `array`  | No       | Optional field-level error details |

```json
{
  "code": "VALIDATION_ERROR",
  "message": "The request body is invalid.",
  "details": [
    {
      "field": "string",
      "message": "string"
    }
  ]
}
```
