---
title: "Update project"
description: "Updates a project's configuration."
label: "PUT /projects/{projectId}"
order: 14
tags:
  - "Projects"
---

# Update project

`PUT /projects/{projectId}`

Updates a project's configuration.

## Parameters

| Name | In | Required | Type | Description |
| --- | --- | --- | --- | --- |
| `projectId` | path | Yes | `string<uuid>` | Unique project identifier |

## Request body

Required · `application/json`

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `name` | `string` | No | Updated name |
| `description` | `string` | No | Updated description |
| `baseUrl` | `string<uri>` | No | Updated base URL |

```json
{
  "name": "string",
  "description": "string",
  "baseUrl": "string"
}
```

<ApiPlayground id="updateproject" method="PUT" url="https://api.foldocs.dev/projects/{projectId}" body="%7B%0A%20%20%22name%22%3A%20%22string%22%2C%0A%20%20%22description%22%3A%20%22string%22%2C%0A%20%20%22baseUrl%22%3A%20%22string%22%0A%7D" />

## Request examples

```bash tab="cURL"
curl --request PUT --url "https://api.foldocs.dev/projects/{projectId}" --header 'content-type: application/json' --data '{"name":"string","description":"string","baseUrl":"string"}'
```

```ts tab="TypeScript"
const response = await fetch("https://api.foldocs.dev/projects/{projectId}", {
  method: "PUT",
  headers: { "content-type": "application/json" },
  body: JSON.stringify({
    "name": "string",
    "description": "string",
    "baseUrl": "string"
  }),
});

const data = await response.json();
```

```python tab="Python"
import requests

response = requests.request("PUT", "https://api.foldocs.dev/projects/{projectId}", json={"name":"string","description":"string","baseUrl":"string"})
data = response.json()
```

```go tab="Go"
request, err := http.NewRequest("PUT", "https://api.foldocs.dev/projects/{projectId}", strings.NewReader("{\"name\":\"string\",\"description\":\"string\",\"baseUrl\":\"string\"}"))
if err != nil { panic(err) }
request.Header.Set("content-type", "application/json")
response, err := http.DefaultClient.Do(request)
if err != nil { panic(err) }
defer response.Body.Close()
```


## Responses

### 200 Project updated successfully

`application/json`

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `id` | `string<uuid>` | Yes | Unique project identifier |
| `name` | `string` | Yes | Project display name |
| `slug` | `string` | Yes | URL-friendly project identifier |
| `description` | `string` | No | Project description |
| `baseUrl` | `string<uri>` | No | Project base URL |
| `locales` | `array` | No | Supported locales |
| `defaultLocale` | `string` | No | Default locale |
| `createdAt` | `string<date-time>` | Yes | Creation timestamp |
| `updatedAt` | `string<date-time>` | No | Last update timestamp |

```json
{
  "id": "string",
  "name": "My Documentation",
  "slug": "my-docs",
  "description": "API documentation for my project",
  "baseUrl": "https://docs.example.com",
  "locales": [
    "en",
    "es"
  ],
  "defaultLocale": "en",
  "createdAt": "string",
  "updatedAt": "string"
}
```

### 404 Resource not found

`application/json`

| Field | Type | Required | Description |
| --- | --- | --- | --- |
| `code` | `string` | Yes | Machine-readable error code |
| `message` | `string` | Yes | Human-readable error message |
| `details` | `array` | No | Optional field-level error details |

```json
{
  "code": "VALIDATION_ERROR",
  "message": "The request body is invalid.",
  "details": [
    {
      "field": "string",
      "message": "string"
    }
  ]
}
```
