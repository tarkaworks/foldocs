---
title: Install with npm
description: Add Foldocs to an existing Foldkit application with npm.
---

# Install with npm

Install Foldocs and its runtime dependencies:

```sh
npm install foldocs foldkit effect
```

Add the Foldocs Vite plugin, then start writing in `content/docs`.
